# Git submodule — the binary uses the generated status_gen.go, not the submodule.
third_party/
# Local dev state
.git
.vscode
*.log
# Node
node_modules/
# Compiled CSS (produced inside Docker by the css-builder stage).
static/css/site.css
